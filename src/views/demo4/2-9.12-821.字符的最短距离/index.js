function short(S, C) {
  const data = S.split("");
  const res = [];
  for (let index = 0; index < data.length; index++) {
    let flag = -1;
    if (data[index] === C) {
      flag = index;
    }
    if (flag === -1) {
      res.push[index];
    }
  }
}
